services:
  backend:
    environment:
      OLLAMA_URL: ${DOCKER_OLLAMA_URL:-http://host.docker.internal:11434}

  worker:
    environment:
      OLLAMA_URL: ${DOCKER_OLLAMA_URL:-http://host.docker.internal:11434}

# Backend Features

Each folder here is a readable facade over an existing backend capability. These files do not replace the working implementation yet; they give future code a cleaner place to start.

Use `*_run.py` for public calls. Open `Tools/` only when you need builder details.

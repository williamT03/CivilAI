# RAG Navigation Subfeature

Purpose: build ordinance navigation maps, summaries, and structured retrieval payloads for source-grounded answers.

Start with `navigation_run.py`. Builder classes are re-exported from `Tools/navigation.py`.

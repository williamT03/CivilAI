# RAG Database Subfeature

Purpose: structured ordinance schema builders, relational database manager, and document hierarchy persistence.

Start with `database_run.py`. Builder classes are re-exported from `Tools/database.py`.

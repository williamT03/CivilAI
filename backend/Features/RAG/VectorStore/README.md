# RAG VectorStore Subfeature

Purpose: vector indexing and retrieval through Chroma today, with Qdrant migration hooks already exposed.

Start with `vector_store_run.py`. Builder classes are re-exported from `Tools/vector_store.py`.

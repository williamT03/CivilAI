# Auth Feature

Purpose: authentication, account data, saved chat ownership, uploads, subscriptions, and API keys.

Start with `auth_run.py`. The `Tools/` folder re-exports the lower-level auth objects from `backend.app.auth`.

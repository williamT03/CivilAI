# Parser Pipeline

Purpose: parse uploaded ordinance PDFs into chapters, sections, subsections, summaries, relational tables, and vector nodes.

Start with `parser_run.py`. The implementation lives in `Tools/parser.py`.

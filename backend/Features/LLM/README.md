# LLM Feature

Purpose: prompt assembly, answer generation, streaming, custom RAG answers, and provider routing.

Subfeatures:

- `Providers`: OpenAI-compatible, Ollama, and routed provider adapters.
- `Ollama`: Ollama-specific provider facade.

Start with `llm_run.py`.

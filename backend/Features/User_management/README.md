# User Management

Purpose: authentication, account data, saved chat ownership, uploads, subscriptions, and API keys.

Start with `auth_run.py`. The `Tools/` folder contains the lower-level user/auth builders and persistence objects.

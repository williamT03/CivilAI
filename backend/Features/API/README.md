# API Feature

Purpose: versioned platform API routes, request/response schemas, signed uploads, usage endpoints, and authenticated query routes.

Start with `api_run.py`. Use `Tools/routers.py` when you need lower-level FastAPI router objects.

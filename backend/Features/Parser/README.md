# Parser Feature

Purpose: turn ordinance PDFs into structured chapters, sections, subsections, and summaries.

Start with `parser_run.py`. Open `Tools/parser.py` when you need the specific parser builders.

# Chat Feature

Purpose: ordinance question workspace, jurisdiction focus, PDF upload, thread history, message rendering, and cited-answer composition.

Start with `chat_run.ts`. Component and hook exports are grouped in `Tools/`.

# Frontend Features

Each folder is a readable facade over a Next.js application area. Use `*_run.ts` or `index.ts` first, then open `Tools/` only when you need the underlying implementation details.
